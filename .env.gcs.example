# Optional GCS-specific overrides for local development
DOCUMENTS_STORAGE_DRIVER=gcs
GCS_STORAGE_BUCKET=your-docs-bucket
# Prefer ADC or Workload Identity in production instead of static API keys.
# GCS_API_KEY=optional-for-local-only
